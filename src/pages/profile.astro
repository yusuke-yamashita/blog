---
import { Content, frontmatter } from "../data/profile.md";
import AboutLayout from "../layouts/AboutLayout.astro";
---

<AboutLayout frontmatter={{
  title: frontmatter.title || "Profile",
  description: frontmatter.description
}}>
  <Content />
</AboutLayout>