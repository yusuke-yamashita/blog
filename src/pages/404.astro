---
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import LinkButton from "@/components/LinkButton.astro";
import { SITE } from "@/config";
import { Image } from "astro:assets";
import gomaImg from "@/assets/goma.png";
---

<Layout title={`404 Not Found | ${SITE.title}`}>
  <Header />

  <main
    id="main-content"
    class="mx-auto flex max-w-3xl flex-1 items-center justify-center"
  >
    <div class="mb-14 flex flex-col items-center justify-center text-center px-4">
      
      {/* ごまちゃんの画像 */}
      <div class="mb-6">
        <Image 
          src={gomaImg} 
          class="rounded-full w-32 h-32 object-cover shadow-lg border-4 border-skin-card" 
          alt="守り神のごま" 
        />
      </div>

      <h1 class="text-8xl font-bold text-skin-accent tracking-tighter">404</h1>
      
      <p class="mt-4 text-2xl sm:text-3xl font-bold text-skin-base">
        System Error... or Bug?
      </p>
      
      <p class="mt-4 text-lg text-skin-base opacity-90 leading-relaxed">
        お探しのページは見つかりませんでした。<br />
        System Fixerもまだこのリンクは直せていないようです。<br />
        （ごまに願いをかけてみてください)
      </p>

      <LinkButton
        href="/"
        class="mt-8 text-lg border border-skin-line px-6 py-3 hover:bg-skin-card transition-colors rounded-full"
      >
        トップページに戻る (Fix)
      </LinkButton>
    </div>
  </main>

  <Footer />
</Layout>